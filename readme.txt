in this project i made my first git push using:
git init
git status
git add
git commit -m  "discription"
git remote add origin repo.git link
git branch (note your branch nam ewith a green color)
// for now users, git will ask for your login using: username and email or password
git push origin branchName 

for subdequent push or update pn the folder using:
git status
git add .
git commit -m "discription"
git branch
git push origin branchName

if you see a pull request, your files has been uploaded
just click on your branch name to fill the uploaded files 
